# Portfolio launch roadmap

## 1. Final code pass — completed in this package

- Preserve Emmanuel's local fluid clamps and “Frontend Web Developer” positioning.
- Add the first-visit-only hero eyebrow typing sequence.
- Replace OS-dependent logo glyphs with one cross-platform SVG mark.
- Anchor the About word reveal to the floating WhatsApp control and expose tuning constants.
- Add lightweight, single-colour SVG marks to every listed skill.
- Centre dialogs at every viewport size and make their measurements fluid.
- Rebuild the project-modal footer as one full-width primary action above two equal secondary actions.
- Let the project footer enter normal scroll flow on short-height and phone-landscape screens.
- Repair project-carousel navigation with measured geometry and keep screenshots useful at 350px.
- Increase fluid dialog and lightbox space without sacrificing small-screen caps or 44px touch targets.
- Animate local-time digit changes, slow the one-time eyebrow intro, and keep focus rings keyboard-only.
- Finish the About reveal at the viewport top and use Roman numerals for internal step sequences.
- Prepare real-URL-only template checkout actions without publishing placeholder purchase links.
- Add the lightweight Solara-style page scroll treatment without GSAP or nested modal smoothing.
- Keep contact links one-column through 768px and footer legal copy stacked through 600px.
- Enforce source, SEO, responsive, asset, and bundle regression checks.

## 2. Media pass — completed in this package

- Replaced every project placeholder with all 19 desktop and 19 mobile screenshots.
- Renamed every screenshot and video by project, viewport, order, and purpose.
- Exported every raster project image below 100 KiB and visually reviewed contact sheets at the intended proportions.
- Kept all four project previews below 6 MiB with browser-compatible H.264/yuv420p and fast-start metadata.
- Added all ten supplied 960×600 marquee clips below 2 MiB and interleaved them so a project never neighbors itself in either row.
- Corrected the mobile screenshot display ratio to 9:17 and prevented cropping for the one naturally shorter capture.
- Verified every media signature, decoded every full video stream, checked HTTP byte-range delivery, and passed the complete asset audit.

## 3. Merge and deploy

- Include the final 404, error boundary, source-privacy, SEO, service, skill, interaction, and package-card readiness pass.
- Copy the approved source into the GitHub-linked folder without copying `node_modules`, `dist`, or `.git`.
- Preserve the existing `.env` in the GitHub-linked project root; the hand-off ZIP intentionally does not contain credentials.
- Run `npm install`, `npm run check`, and one production preview.
- Add the three EmailJS variables to Netlify and restrict EmailJS allowed origins.
- Deploy, test a real form submission, validate social previews, and run the final mobile/desktop smoke test.
- Unpublish GitHub Pages only after the matching Netlify production link has been verified; keep repositories and Netlify connections intact.

## 4. Commercial launch

- Prepare each sellable template with a product description, feature list, screenshots, preview link, licence terms, and support scope.
- Publish the remaining templates to the selected marketplaces; Lumière's existing Gumroad listing is the baseline, not a duplicate task.
- Add each verified marketplace URL as `purchaseUrl` in `src/data/projects.ts` after its listing is live.
- Stagger the portfolio, Solara, and PrimeNest social posts so each project gets its own attention window.
- Use the deployed portfolio as the destination for targeted client outreach and frontend-role applications.
