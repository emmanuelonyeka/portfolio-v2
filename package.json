{
  "name": "emmanuel-portfolio",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "node scripts/clean-dist.mjs",
    "typecheck": "tsc",
    "verify": "node scripts/verify-project.mjs",
    "build": "npm run typecheck && npm run clean && vite build && npm run audit:build",
    "preview": "vite preview",
    "audit:assets": "node scripts/audit-assets.mjs",
    "audit:build": "node scripts/audit-build.mjs",
    "check": "npm run verify && npm run audit:assets && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "lenis": "^1.3.26",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
